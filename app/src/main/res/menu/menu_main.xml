<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".controllers.MainActivity">

    <item android:id="@+id/action_favoritos"
        android:orderInCategory="100"
        app:showAsAction="never"
        android:title="Favoritos" />

    <item android:id="@+id/action_llamar"
        android:title="Llamar a tienda"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item android:id="@+id/action_buscar"
        android:icon="@drawable/search"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="always|collapseActionView"
        android:title="Buscador" />

    <item android:id="@+id/action_anadir"
        android:icon="@drawable/add"
        android:orderInCategory="100"
        app:showAsAction="ifRoom"
        android:title="" />

</menu>
